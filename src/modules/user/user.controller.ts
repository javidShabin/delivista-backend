import { Request, Response, NextFunction } from "express";
import { IUserModel } from "../authentication/auth.model";

// Get all customers list for admin and seller
export const getAllCustomer = async (
  req: Request,
  res: Response,
  next: NextFunction
) => {
    try {
        // 
    } catch (error) {
        
    }
};

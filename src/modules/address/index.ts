import addressRouter from "./address.routes"
import * as addressController from "./address.controller"
import * as addressModel from "./address.model"
import * as addressInterface from "./address.interface"
import * as addressValidation from "./address.validation"

export {
    addressRouter,
    addressController,
    addressModel,
    addressInterface,
    addressValidation
}